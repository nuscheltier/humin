//Categories are the go to place for tags and things like that. It's a one-fits-all.
//Effectively you can use it for Clans, Organizations and other things.

pub struct Categories {
    categories: Vec<u64>
}

impl Categories {
}
